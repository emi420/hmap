
const FIRMSLayer = (id, data) => {
    return [{
        id: id,
        data: data,
        circlePaint: {
            "circle-radius": 4,
            "circle-color": "red",
        }
    }];
}

export default FIRMSLayer;
