
const GOESLayer = (id, data) => {
    return [{
        id: id,
        data: data,
        circlePaint: {
            "circle-radius": 4,
            "circle-color": "yellow",
        }
    }];
}

export default GOESLayer;
